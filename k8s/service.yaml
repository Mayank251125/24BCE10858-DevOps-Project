apiVersion: v1
kind: Service
metadata:
  name: vit-taskmanager-service
  labels:
    app: vit-taskmanager
spec:
  type: NodePort
  selector:
    app: vit-taskmanager
  ports:
  - port: 80
    targetPort: 8081
    nodePort: 30080
    protocol: TCP
