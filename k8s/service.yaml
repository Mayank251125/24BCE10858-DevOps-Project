apiVersion: v1
kind: Service
metadata:
  name: vit-techfest-service
  labels:
    app: vit-techfest
spec:
  type: NodePort
  selector:
    app: vit-techfest
  ports:
  - port: 80
    targetPort: 8081
    nodePort: 30080
    protocol: TCP
