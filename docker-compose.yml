version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vit_techfest_app
    ports:
      - "8082:8081"
    restart: always
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - GRAPHITE_HOST=host.docker.internal
